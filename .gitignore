.idea/
build/
web/node_modules/
web/dist/
# Screenshots produced by the Chromium smoke scripts; CI uploads them as run artifacts.
web/browser-artifacts/
web/android/
web/*.tsbuildinfo
*.tgz

web/dist-electron/
web/release/
web/release-*/
.smoke-state/

# The bridge has no dependencies; CI runs its tests without installing any.
bridge/package-lock.json

# Third-party Claude Code skills installed locally (ui-ux-pro-max and friends). Tooling, not
# product code, and 11MB of vendored data files.
.claude/skills/
